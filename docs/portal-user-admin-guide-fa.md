# راهنمای کامل AGTPS Portal

## معرفی

AGTPS Portal یک پرتال داخلی سازمانی برای کارکنان و مدیران است. این سامانه شامل صفحه اصلی پرتال، پنل مدیریت، ماژول‌های اطلاع‌رسانی، سامانه‌ها، سایت‌ها، نقشه ایران، تقویم، آموزش، فایل‌شیر، بکاپ، مرکز اعلان‌ها و ابزارهای آماده‌سازی انتشار است.

محل درج اسکرین‌شات: صفحه اصلی پرتال

## صفحه اصلی پرتال

صفحه اصلی پرتال برای استفاده روزانه کارکنان طراحی شده است و شامل بخش‌های زیر است:

- هدر سازمانی با لوگو، نام شرکت، ساعت و تاریخ شمسی.
- نمایش نام کاربر و دسترسی به پنل کاربری.
- اسلایدر پیام‌های مهم سازمانی.
- نقشه سایت‌ها و موقعیت‌های عملیاتی.
- سامانه‌های سازمانی و لینک‌های دسترسی سریع.
- اطلاعیه‌ها و اخبار.
- تقویم جلسات، مناسبت‌ها، یادآوری‌ها و تسک‌ها.
- دفترچه یادداشت شخصی.
- فایل‌شیر سازمانی در صورت فعال بودن ماژول.
- آموزش‌ها و محتوای آموزشی در صورت فعال بودن ماژول.
- نظرسنجی و رای‌گیری‌های فعال.

محل درج اسکرین‌شات: هدر و ویجت‌های صفحه اصلی

## پنل مدیریت

پنل مدیریت برای مدیران سامانه و مدیران محتوایی طراحی شده است. دسترسی به بخش‌ها بر اساس نقش‌ها، permissionها و وضعیت فعال بودن ماژول‌ها کنترل می‌شود.

محل درج اسکرین‌شات: داشبورد مدیریت

## داشبورد مدیریت

داشبورد مدیریت نمای سریع وضعیت کلی پرتال را نشان می‌دهد:

- تعداد سامانه‌ها، سایت‌ها، کاربران و محتواها.
- وضعیت ماژول‌ها.
- وضعیت سیستم‌ها و سرویس‌های مانیتور شده.
- نظرسنجی‌ها و رای‌گیری‌های فعال.
- بخش Production Readiness برای بررسی آمادگی انتشار.

محل درج اسکرین‌شات: بخش Production Readiness

## مدیریت سامانه‌ها

در این بخش ادمین می‌تواند سامانه‌های داخلی و سازمانی را ثبت و دسته‌بندی کند.

قابلیت‌ها:

- افزودن، ویرایش و حذف سامانه.
- انتخاب دسته‌بندی.
- تعیین وضعیت سامانه.
- انتخاب آیکن پیش‌فرض یا آپلود آیکن اختصاصی.
- اتصال سامانه به سایت‌ها.
- تعریف نوع شبکه مانند اینترنت، اینترانت یا VPN.

محل درج اسکرین‌شات: صفحه مدیریت سامانه‌ها

## دسته‌بندی‌ها

دسته‌بندی‌ها برای مرتب‌سازی سامانه‌ها و نمایش بهتر در پرتال استفاده می‌شوند.

قابلیت‌ها:

- ایجاد دسته‌بندی.
- ویرایش نام، رنگ و آیکن.
- فعال یا غیرفعال کردن دسته‌بندی.

## سایت‌ها و نقشه ایران

ماژول سایت‌ها برای تعریف موقعیت‌های سازمانی مانند دفتر تهران و سایت عسلویه استفاده می‌شود.

قابلیت‌ها:

- تعریف سایت‌ها.
- نمایش سایت‌ها روی نقشه ایران.
- نمایش مارکر سایت‌ها.
- امکان توسعه نقشه سه‌بعدی و داده GIS در فازهای بعدی.

محل درج اسکرین‌شات: نقشه ایران در صفحه اصلی

## اخبار و اطلاعیه‌ها

اخبار و اطلاعیه‌ها برای اطلاع‌رسانی رسمی سازمانی استفاده می‌شوند.

قابلیت‌ها:

- افزودن خبر.
- افزودن اطلاعیه.
- تاریخ شروع و پایان نمایش برای اطلاعیه.
- نمایش مودال انیمیشنی در پرتال.
- اتصال اطلاعیه منتشرشده به Notification Center.

محل درج اسکرین‌شات: مودال مشاهده اطلاعیه

## تقویم جلسات و فضای کاری

این بخش شامل جلسات، یادداشت‌ها، تسک‌ها و یادآوری‌ها است.

قابلیت‌ها:

- ساخت جلسه.
- جلسه عمومی یا خصوصی.
- افزودن اعضا از کاربران و دایرکتوری.
- ارسال اعلان دعوت جلسه.
- تقویم شمسی.
- نمایش روزانه، ماهانه و سالانه.
- یادداشت شخصی هر کاربر.
- تسک و یادآوری با زمان اعلان.

محل درج اسکرین‌شات: تقویم ماهانه پیشرفته

## کاربران، اکتیو دایرکتوری و دسترسی‌ها

پرتال برای کاربران داخلی و کاربران Active Directory آماده شده است.

قابلیت‌ها:

- مدیریت کاربران داخلی.
- تغییر رمز کاربران داخلی.
- جلوگیری از تغییر رمز کاربران Active Directory.
- تعریف گروه‌ها.
- عضویت چندگانه کاربر در گروه‌ها.
- نقش‌ها و permissionها.
- نمایش وضعیت اتصال Active Directory.
- اجبار تکمیل کد پرسنلی و تاریخ تولد.

محل درج اسکرین‌شات: صفحه کاربران و دایرکتوری

## مرکز اعلان‌ها

Notification Center مسئول ارسال تمام اعلان‌ها و ایمیل‌ها است. ماژول‌ها نباید مستقیم ایمیل ارسال کنند.

قابلیت‌ها:

- تعریف چند SMTP.
- SMTP اصلی و پشتیبان.
- تست اتصال SMTP و ارسال ایمیل تست.
- قالب‌های ایمیل قابل ویرایش.
- صف ایمیل.
- پردازش خودکار صف.
- قوانین اعلان برای روشن یا خاموش کردن کانال‌ها.

محل درج اسکرین‌شات: تب قوانین در Notification Center

## مرکز بکاپ و Restore

Backup Center برای حفاظت از دیتابیس و فایل‌های مهم استفاده می‌شود.

قابلیت‌ها:

- بکاپ دستی.
- بکاپ خودکار ساعتی، روزانه، هفتگی و ماهانه.
- بکاپ دیتابیس، فایل‌ها یا بکاپ کامل.
- دانلود امن بکاپ.
- ارسال نتیجه بکاپ از Notification Center.
- Restore دیتابیس و uploads.
- گرفتن بکاپ اضطراری قبل از Restore.
- تاریخچه Restore.

محل درج اسکرین‌شات: صفحه Backup Center و مودال Restore

## فایل‌شیر SMB

ماژول فایل‌شیر برای نمایش فولدرهای شبکه در پرتال طراحی شده است.

قابلیت‌ها:

- تعریف Share.
- کنترل دسترسی بر اساس کاربر و گروه.
- نمایش فایل‌ها در پرتال.
- پیش‌نمایش عکس، ویدیو و اسناد.
- ثبت audit برای عملیات فایل.

محل درج اسکرین‌شات: صفحه فایل‌شیر در پرتال

## آموزش

ماژول آموزش برای مدیریت محتوای آموزشی سازمانی استفاده می‌شود.

قابلیت‌ها:

- افزودن آموزش.
- چند فایل با فرمت‌های مختلف.
- نمایش ویدیو، عکس و اسناد داخل پرتال.
- محدودیت حجم فایل قابل تنظیم.
- دسته‌بندی و مدیریت دوره‌ها.

محل درج اسکرین‌شات: صفحه مشاهده آموزش

## نظرسنجی و رای‌گیری

این ماژول برای رای‌گیری و نظرسنجی سازمانی استفاده می‌شود.

قابلیت‌ها:

- Poll و Survey در یک موتور مشترک.
- سوالات چندصفحه‌ای برای نظرسنجی.
- رای‌گیری تک‌گزینه‌ای و چندگزینه‌ای.
- حالت ناشناس یا نام‌دار.
- مشارکت اجباری.
- گزارش نتایج.
- clone و ویرایش امن.

محل درج اسکرین‌شات: صفحه پاسخ به نظرسنجی

## وضعیت سیستم‌ها

این بخش برای بررسی وضعیت سامانه‌های داخلی و خارجی استفاده می‌شود.

قابلیت‌ها:

- Health Check دستی.
- HTTP Check.
- TCP Port Check.
- Ping.
- SMB Check.
- زمان‌بندی بررسی.
- نمایش وضعیت سالم، اختلال یا قطع.

محل درج اسکرین‌شات: صفحه وضعیت سیستم‌ها

## ارتباط مستقیم مدیران

این ماژول پایه ارتباط مستقیم با مدیرعامل و مدیران بخش‌ها را فراهم می‌کند.

قابلیت‌های فاز فعلی:

- تعریف فقط یک مدیرعامل.
- تعریف مدیران بخش‌های مختلف.
- انتخاب مدیر از کاربران داخلی یا کاربران دایرکتوری.
- فعال یا غیرفعال کردن مدیر.
- تعریف کلمات ممنوعه.
- نرمال‌سازی فارسی برای فیلتر کلمات.
- آماده‌سازی اسکلت مکالمه‌ها، وضعیت‌ها، سطح محرمانگی و اولویت.
- ذخیره پیام به صورت payload رمزنگاری‌شده با AES-256-GCM سمت سرور.
- امکان غیرفعال کردن ارسال پیام با مقدار `DIRECT_COMMUNICATION_MESSAGING_ENABLED=false`.
- نیاز به تنظیم `DIRECT_COMMUNICATION_ENCRYPTION_KEY` در محیط عملیاتی.
- صفحه «پیام‌های من» در داشبورد برای ارسال پیام توسط کاربران عادی.
- صفحه «پیام‌های من» در داشبورد برای مشاهده پیام‌های دریافتی مدیر یا مدیرعامل.
- مشاهده جزئیات مکالمه و زنجیره پیام‌ها فقط برای فرستنده همان مکالمه یا مدیر دریافت‌کننده همان مکالمه.
- پاسخ به مکالمه از صفحه «پیام‌های من» برای فرستنده و مدیر دریافت‌کننده.
- نمایش فقط نوتیفیکیشن پیام در صفحه اصلی پورتال؛ متن و لیست پیام‌ها در صفحه اصلی پورتال نمایش داده نمی‌شود.
- محدود شدن صفحه تنظیمات ارتباط مدیران به ادمین یا کاربر دارای دسترسی `ceo.settings.manage`.

سطح دسترسی:

- کاربر عادی: فقط ارسال پیام و مشاهده پیام‌های ارسالی خودش از مسیر `/admin/direct-messages`.
- مدیر یا مدیرعامل: ارسال پیام، مشاهده پیام‌های ارسالی خودش، و مشاهده پیام‌های دریافتی مربوط به خودش از مسیر `/admin/direct-messages`.
- ادمین: مدیریت مدیرعامل، مدیران بخش‌ها، کلمات ممنوعه و تنظیمات امنیتی از مسیر `/admin/direct-communication`.
- ادمین نباید برای مشاهده متن پیام‌های محرمانه یا ناشناس مسیر جداگانه داشته باشد.
- ادمین فقط در صورت فرستنده بودن یا مدیر دریافت‌کننده بودن همان مکالمه، متن همان مکالمه را می‌بیند.

موارد فاز بعدی:

- Recovery Key.
- رمزنگاری End-to-End واقعی.
- مهاجرت از رمزنگاری سمت سرور به رمزنگاری End-to-End در صورت نیاز امنیتی سطح بالا.
- پشتیبانی کامل از پیوست، تصویر و Voice.

محل درج اسکرین‌شات: صفحه پیام‌های من

محل درج اسکرین‌شات: صفحه تنظیمات ارتباط مدیران

## Audit Log

Audit Log برای ثبت عملیات حساس استفاده می‌شود.

ثبت می‌شود:

- ورود موفق و ناموفق.
- تغییر کاربر و رمز.
- ساخت، حذف و Restore بکاپ.
- تغییر قوانین اعلان.
- تعریف مدیران ارتباط مستقیم.
- تعریف کلمات ممنوعه.

ثبت نمی‌شود:

- متن پیام‌های محرمانه.
- رمزها.
- محتوای فایل‌ها.
- محتوای ایمیل‌ها.

محل درج اسکرین‌شات: صفحه Audit Log

## ماژول‌ها

پرتال از مدل ماژولار پشتیبانی می‌کند. هر ماژول می‌تواند فعال یا غیرفعال شود و در صورت غیرفعال بودن از منوها و سطح کاربری حذف می‌شود.

## چک‌لیست انتشار

- تنظیم `DATABASE_URL`.
- تنظیم `JWT_SECRET`.
- تنظیم `PORTAL_URL`.
- بررسی volume مربوط به uploads.
- بررسی volume مربوط به backups.
- اجرای migration.
- تست Health Readiness.
- گرفتن بکاپ موفق.
- تست دانلود بکاپ.
- تست Restore در محیط تست.
- تست SMTP.
- بررسی Notification Rules.
- بررسی Audit Log.

## جمع‌بندی

AGTPS Portal اکنون یک پرتال سازمانی ماژولار با قابلیت مدیریت محتوا، سامانه‌ها، جلسات، اعلان‌ها، بکاپ، Restore، فایل‌شیر، آموزش، نظرسنجی و کنترل‌های آماده انتشار است. توسعه‌های بعدی باید روی امنیت ارتباط مستقیم رمزنگاری‌شده، نقشه GIS پیشرفته و تکمیل اتصال Active Directory متمرکز شود.
